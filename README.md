# Pranav-Maheshwari-26-A04-068
<h1> Round 3 - UGV </h1>
<h2> Task - 1 </h2>
<h3> 25/08/2026 </h3>
<p font size = "3"> Today, I learned to use the basic commands of git and github. I learned the commands which shall be sued daily to save my progress. I learned to use these commands both through github and thorugh command line.</font></p>
<h2> Task - 2 </h2>
<h3> 26/08/2026 </h3>
<p font size = "3"> Today, I started doing the task 2 which is lane detection. Since, I knew basics of python, I referred a youtube video for the same so that along with writing the code I would be able to understand it as well. First thing I did was to install openCV through command line which invloved the command "pip install -contrib opencv". After it was successfully installed, I installed matplotlib as well and then I started writing the code. First thing which the video made my do was to convert the image to grayscale. I understood its importance as I read about it in round 2 of UGV from the sources provided in the pdf. After converting the image to grayscale, I learned to blur the image so as to smoothen the edges and remove any unnecessary edges which might intervene in the form of turns. After that I used the functions "cv2.Canny" to trace the edges which showed a significant change in pixel values. This method gave me an putline of image.</font></p>
<p font size = "3"> The next thing I did was to find the region of interest i.e the region for which I had to detect the lanes. The first thing I did was to get the image on graph using matplotlib. This gave me the coordinates of the concerned region. Saving the iamge and using it as reference, I created a copy of image and made it completely black. Then I added a white triangle on it using the coordinates of the concerned region which I got through matplotlib. After the triangle was formed I had to club it with the orginal image so that I can get an image which contained only my region of interest. This was done using "cv2.bitwise_and" function. The code ran successfully and I got my region of interest. Now the next task was to add lines on it.</font></p>
<h3> 27/08/2026 </h3>
<p font size = "3"> Since I got the outline of the image, I started working on how to get the lines on it.First of all, I created a black image with same dimesnions as that of original image. Then I defined the coordinates of start and end point of the line as well as the colour of the line. After that I created a function "make_coordinates" in which I defined the coordinates of the line which I had declared before. The result gave me the path highlighted. However instead of getting a single line I was getting pieces of it which means I had to average out the lines so that I can get only 2 lines enclosing the path between. This I did with the help of "average_slope_intercept" function and finally Task 2 was completed.</font></p>
<h3> 28/08/2026 </h3>
<p font size = "3"> Although task 2 was completed, however, one change had to be made. Upon consulting the admin of group I got to know that the code for all images had to be compiled in file. This involved the use of "for loop". But at the same time it required a few changes to be made. Since, the arrays for every image for different, therefore, I created a separate list named "polygons" which contained the arrays of each image in order. The same I did by creating a list named "list_path" which included the paths of image. Since, the slope conditons and the y2 coordinates for each image were also different, therefore, I made separate lists for them as well and integrated them with the for loop via iterations. As a result, all the images were being displayed one by one as output. I also added the function "cv2.imwrite" which automatically stored the image in the folder/path specified. Now, task  was completed in true sense.</font></p>
<h2> Task 3 </h2>
<h3> 28/06/2026 </h3>
<p font size = "3">Now was task 3. I searched the net for some videos regarding it but didn't get any. So I used all the three AIs i.e. Claude, ChatGPT and Google Gemini to get the code as well as the explanation along with it. Since, the goal was to detect potholes and obstacles, first of all, I found the ROI by ignoring certain parts from top and bottom of the image to avoid unnecessary outlines. Then I added the code for finding potholes using certain measurements as provided by the AI.Also a function was created to avoid overlapping of found obstacles. The same process was repeated for finding obstacles. After that colours and labels were specified for the rectangles used to detect the potholes and obstacles and finally these rectangles and labels were added onto the original image. I created a list of "image_path" which contained the paths of the image and then integrated it with "for loop" to display the output of all images one by one and saving them simultaneiously in the specified folder.</font></p>
<h2> Task 4 </h2>
<h3> 28/06/2026 </h3>
<p font size = "3">Now came the task4 which was detecting aerial path. As no videos were available I consulted AI only. It suggested me to use A* along with checkpoints method due to the image stauration which was mostly gray. The pipeline was to first mark the checkpoints that the object ca travel which I enetered manually. I got those points using "matplotlib.pylot". Second was to connect these paths using lines whose specifications were added to the code. Then as a safety measure the obstacles weere enlarged a bit so that the checkpoints do not get very close to the obstacles. Then came the role of A*. Its algorithm was calulcated. After that safe points were calculated. Then a list was created in which those safe points were appended and,then, the final path was formulated. </font></p>